import React from 'react';
import Hero from '@/components/Hero';
import GameBoard from '@/components/GameBoard';

const Home: React.FC = () => {
  return (
    <>
      <GameBoard />
    </>
  );
};

export default Home;
